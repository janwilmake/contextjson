{
  "name": "contextjson",
  "version": "0.0.5",
  "bin": "cli.js",
  "repository": "https://github.com/janwilmake/contextjson",
  "scripts": {
    "pub": "npm publish && git add . && git commit -m 'improvements' && git push && wrangler deploy"
  },
}
